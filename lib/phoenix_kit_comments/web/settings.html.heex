<div class="container flex flex-col mx-auto px-4 py-6 overflow-x-hidden">
  <.admin_page_header
    back={PhoenixKit.Utils.Routes.path("/admin/settings")}
    title={gettext("Comments Settings")}
    subtitle={gettext("Configure the standalone comments module")}
  />

  <%!-- Settings Form --%>
  <div class="max-w-4xl w-full mx-auto space-y-8">
    <div class="card bg-base-100 shadow-xl">
      <div class="card-body overflow-hidden">
        <.form for={%{}} phx-submit="save" class="space-y-6">
          <.settings_section_header
            icon="hero-cog-6-tooth"
            title={gettext("General")}
            class="pt-0"
          />

          <%!-- Enable Comments --%>
          <div class="fieldset w-full">
            <label class="label cursor-pointer justify-start gap-4">
              <input type="hidden" name="settings[comments_enabled]" value="false" />
              <input
                type="checkbox"
                name="settings[comments_enabled]"
                value="true"
                class="toggle toggle-primary shrink-0"
                checked={@comments_enabled == "true"}
              />
              <span class="fieldset-legend font-medium">{gettext("Enable Comments")}</span>
            </label>
            <div class="label pt-0">
              <span class="fieldset-label text-xs text-base-content/50">
                {gettext("Enable the standalone comments system across all content types")}
              </span>
            </div>
          </div>

          <%!-- Rich-Text Editor --%>
          <div class="fieldset w-full">
            <label class="label cursor-pointer justify-start gap-4">
              <input type="hidden" name="settings[comments_rich_text]" value="false" />
              <input
                type="checkbox"
                name="settings[comments_rich_text]"
                value="true"
                class="toggle toggle-primary shrink-0"
                checked={@comments_rich_text == "true"}
              />
              <span class="fieldset-legend font-medium">{gettext("Rich-Text Editor")}</span>
            </label>
            <div class="label pt-0">
              <span class="fieldset-label text-xs text-base-content/50">
                {gettext(
                  "Use the Leaf rich-text editor in the comment composer. Turn off to fall back to a plain textarea — do this if the host app hasn't registered Leaf's JS hook (otherwise the editor hangs on its loading text)."
                )}
              </span>
            </div>
          </div>

          <.settings_section_header icon="hero-shield-check" title={gettext("Moderation")} />

          <%!-- Require Moderation --%>
          <div class="fieldset w-full">
            <label class="label cursor-pointer justify-start gap-4">
              <input type="hidden" name="settings[comments_moderation]" value="false" />
              <input
                type="checkbox"
                name="settings[comments_moderation]"
                value="true"
                class="toggle toggle-warning shrink-0"
                checked={@comments_moderation == "true"}
              />
              <span class="fieldset-legend font-medium">{gettext("Require Moderation")}</span>
            </label>
            <div class="label pt-0">
              <span class="fieldset-label text-xs text-base-content/50">
                {gettext("New comments must be approved before appearing publicly")}
              </span>
            </div>
          </div>

          <.settings_section_header
            icon="hero-adjustments-horizontal"
            title={gettext("Limits")}
          />

          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-4">
            <div class="fieldset w-full">
              <label class="label">
                <span class="fieldset-legend text-base font-medium">
                  {gettext("Max Thread Depth")}
                </span>
              </label>
              <input
                type="number"
                name="settings[comments_max_depth]"
                value={@comments_max_depth}
                min="1"
                max="50"
                class="input w-full focus:input-primary"
              />
              <div class="label">
                <span class="fieldset-label text-xs text-base-content/50">
                  {gettext("Maximum nesting level for reply threads (default: 10)")}
                </span>
              </div>
            </div>

            <div class="fieldset w-full">
              <label class="label">
                <span class="fieldset-legend text-base font-medium">
                  {gettext("Max Comment Length")}
                </span>
              </label>
              <input
                type="number"
                name="settings[comments_max_length]"
                value={@comments_max_length}
                min="100"
                max="100000"
                class="input w-full focus:input-primary"
              />
              <div class="label">
                <span class="fieldset-label text-xs text-base-content/50">
                  {gettext("Maximum characters per comment (default: 10,000)")}
                </span>
              </div>
            </div>
          </div>

          <.settings_section_header icon="hero-film" title={gettext("Giphy Integration")} />

          <%!-- Enable Giphy Picker --%>
          <div class="fieldset w-full">
            <label class="label cursor-pointer justify-start gap-4">
              <input type="hidden" name="settings[comments_giphy_enabled]" value="false" />
              <input
                type="checkbox"
                name="settings[comments_giphy_enabled]"
                value="true"
                class="toggle toggle-primary shrink-0"
                checked={@comments_giphy_enabled == "true"}
              />
              <span class="fieldset-legend font-medium">{gettext("Enable Giphy Picker")}</span>
            </label>
            <div class="label pt-0">
              <span class="fieldset-label text-xs text-base-content/50">
                {gettext("Show a GIF picker in the comment form. Requires a valid API key below.")}
              </span>
            </div>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-4">
            <div class="fieldset w-full">
              <label class="label">
                <span class="fieldset-legend text-base font-medium">{gettext("Giphy API Key")}</span>
              </label>
              <input
                type="password"
                name="settings[comments_giphy_api_key]"
                value={@comments_giphy_api_key}
                autocomplete="off"
                placeholder={gettext("Paste your Giphy API key")}
                class="input w-full focus:input-primary"
              />
              <div class="label">
                <span class="fieldset-label text-xs text-base-content/50">
                  {gettext("Stored in the database. Leave blank to disable.")}
                </span>
              </div>
            </div>

            <div class="fieldset w-full">
              <label class="label">
                <span class="fieldset-legend text-base font-medium">{gettext("Content Rating")}</span>
              </label>
              <label class="select w-full focus:select-primary">
                <select name="settings[comments_giphy_rating]">
                  <option value="g" selected={@comments_giphy_rating == "g"}>G</option>
                  <option value="pg" selected={@comments_giphy_rating == "pg"}>PG</option>
                  <option value="pg-13" selected={@comments_giphy_rating == "pg-13"}>
                    PG-13
                  </option>
                  <option value="r" selected={@comments_giphy_rating == "r"}>R</option>
                </select>
              </label>
              <div class="label">
                <span class="fieldset-label text-xs text-base-content/50">
                  {gettext("Maximum rating returned by Giphy search (default: G)")}
                </span>
              </div>
            </div>
          </div>

          <.settings_section_header icon="hero-paper-clip" title={gettext("Attachments")} />

          <%!-- Enable Comment Attachments --%>
          <div class="fieldset w-full">
            <label class="label cursor-pointer justify-start gap-4">
              <input type="hidden" name="settings[comments_attachments_enabled]" value="false" />
              <input
                type="checkbox"
                name="settings[comments_attachments_enabled]"
                value="true"
                class="toggle toggle-primary shrink-0"
                checked={@comments_attachments_enabled == "true"}
              />
              <span class="fieldset-legend font-medium">
                {gettext("Enable Comment Attachments")}
              </span>
            </label>
            <div class="label pt-0">
              <span class="fieldset-label text-xs text-base-content/50">
                {gettext(
                  "Allow users to attach images, video, audio, or files to comments. Voice messages can also be recorded in-browser. Uses the global storage backend configured under Settings → Storage."
                )}
              </span>
            </div>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-x-6 gap-y-4">
            <div class="fieldset w-full">
              <label class="label">
                <span class="fieldset-legend text-base font-medium">
                  {gettext("Max attachments per comment")}
                </span>
              </label>
              <input
                type="number"
                name="settings[comments_max_attachments]"
                value={@comments_max_attachments}
                min="1"
                max="10"
                class="input w-full focus:input-primary"
              />
              <div class="label">
                <span class="fieldset-label text-xs text-base-content/50">
                  {gettext("Per-comment cap (1–10). Default: 4")}
                </span>
              </div>
            </div>

            <div class="fieldset w-full">
              <label class="label">
                <span class="fieldset-legend text-base font-medium">
                  {gettext("Max file size (MB)")}
                </span>
              </label>
              <input
                type="number"
                name="settings[comments_attachment_max_size_mb]"
                value={@comments_attachment_max_size_mb}
                min="1"
                max="500"
                class="input w-full focus:input-primary"
              />
              <div class="label">
                <span class="fieldset-label text-xs text-base-content/50">
                  {gettext("Clamped against the global storage cap. Default: 20")}
                </span>
              </div>
            </div>
          </div>

          <%!-- Action Buttons --%>
          <div class="flex gap-3 pt-4">
            <button
              type="submit"
              phx-disable-with={gettext("Saving…")}
              class={["btn btn-primary flex-1", if(@saving, do: "loading", else: "")]}
              disabled={@saving}
            >
              <%= if @saving do %>
                {gettext("Saving...")}
              <% else %>
                {gettext("Save Settings")}
              <% end %>
            </button>
            <button
              type="button"
              phx-click="reset_defaults"
                    phx-disable-with={gettext("Resetting...")}
              class="btn btn-outline"
              data-confirm={
                gettext(
                  "Reset ALL comments settings to their defaults? Moderation, limits, Giphy and attachment options will be replaced. This cannot be undone."
                )
              }
            >
              <.icon name="hero-arrow-path" class="w-4 h-4 mr-2" /> {gettext("Reset")}
            </button>
          </div>
        </.form>
      </div>
    </div>

    <%!-- Resource Link Patterns (outside main form to avoid nested forms) --%>
    <div class="card bg-base-100 shadow-xl mb-6">
      <div class="card-body overflow-hidden">
        <h2 class="card-title text-lg mb-2 flex-wrap">
          <.icon name="hero-link" class="w-5 h-5" /> {gettext("Resource Link Patterns")}
        </h2>
        <p class="text-sm text-base-content/60 mb-4 break-words">
          {gettext(
            "Configure URL patterns for resource types so comments link to their source in the admin page."
          )}
          <%!-- One sentence, one msgid. This was five fragments assembled
               around <code> tokens — "Use" / "for resource ID," / "or" —
               which freezes word order to English and hands a translator
               "for resource ID," with no context. The placeholders carry
               the tokens instead, so a locale can put them anywhere. --%>
          {gettext(
            "Use %{uuid} for the resource ID, %{metadata} for values from comment metadata, and %{prefix} to include the site URL prefix.",
            uuid: ":uuid",
            metadata: ":metadata.KEY",
            prefix: ":prefix"
          )}
        </p>
        <p class="text-xs text-base-content/40 mb-4 break-words">
          {gettext("Example: %{simple} or %{prefixed}",
            simple: "/products/:uuid",
            prefixed: ":prefix/admin/shop/:uuid"
          )}
        </p>

        <%!-- Existing patterns --%>
        <%= if map_size(@resource_paths) > 0 do %>
          <%!-- Desktop: table view --%>
          <div class="hidden md:block mb-4">
            <table class="table table-sm table-fixed w-full">
              <thead>
                <tr>
                  <th>{gettext("Resource Type")}</th>
                  <th>{gettext("Path Template")}</th>
                  <th>{gettext("Display Template")}</th>
                  <th class="w-20"></th>
                </tr>
              </thead>
              <tbody>
                <%= for {resource_type, config} <- Enum.sort(@resource_paths) do %>
                  <% display_path = extract_path(config) %>
                  <% display_title = extract_title(config) %>
                  <%= if @editing_resource_type == resource_type do %>
                    <tr>
                      <td>
                        <span class="badge badge-ghost badge-sm">{resource_type}</span>
                      </td>
                      <td colspan="3">
                        <div
                          id={"edit-path-#{resource_type}"}
                          phx-hook="PhoenixKitCommentsInsertAtCursor"
                        >
                          <.form
                            for={%{}}
                            phx-submit="save_resource_path"
                            phx-change="live_edit_path"
                            class="space-y-2"
                          >
                            <div>
                              <label class="label py-0">
                                <span class="fieldset-legend text-xs font-semibold">
                                  {gettext("Path")}
                                </span>
                              </label>
                              <textarea
                                name="resource_path[path_template]"
                                class="textarea textarea-sm w-full min-h-[4rem]"
                                rows="2"
                                required
                              ><%= @editing_path_value %></textarea>
                            </div>
                            <div>
                              <label class="label py-0">
                                <span class="fieldset-legend text-xs font-semibold">
                                  {gettext("Display title")}
                                </span>
                              </label>
                              <input
                                type="text"
                                name="resource_path[title_template]"
                                value={@editing_title_value}
                                placeholder={gettext("e.g. :metadata.slug - Ch. :metadata.chapter")}
                                class="input input-sm w-full"
                              />
                            </div>
                            <%= if keys = Map.get(@metadata_keys_by_type, resource_type) do %>
                              <div class="text-xs text-base-content/60 flex flex-wrap items-center gap-1">
                                <span class="font-semibold">{gettext("Available fields:")}</span>
                                <%= for key <- keys do %>
                                  <span
                                    class="badge badge-ghost badge-xs font-mono gap-0.5"
                                    data-field={":metadata.#{key}"}
                                  >
                                    :metadata.{key}
                                  </span>
                                <% end %>
                              </div>
                            <% end %>
                            <div class="flex justify-end gap-2">
                              <button
                                type="button"
                                phx-click="cancel_edit_resource_path"
                                class="btn btn-ghost btn-xs"
                              >
                                {gettext("Cancel")}
                              </button>
                              <button type="submit"
              phx-disable-with={gettext("Saving…")} class="btn btn-primary btn-xs">
                                {gettext("Save")}
                              </button>
                            </div>
                          </.form>
                        </div>
                      </td>
                    </tr>
                  <% else %>
                    <tr>
                      <td>
                        <span class="badge badge-ghost badge-sm">{resource_type}</span>
                      </td>
                      <td class="max-w-[280px]">
                        <code class="text-sm break-all">{display_path}</code>
                      </td>
                      <td class="max-w-[200px]">
                        <%= if display_title != "" do %>
                          <code class="text-sm break-all">{display_title}</code>
                        <% else %>
                          <span class="text-base-content/40 text-xs">
                            {gettext("UUID (default)")}
                          </span>
                        <% end %>
                      </td>
                      <td>
                        <div class="flex gap-1">
                          <button
                            type="button"
                            phx-click="edit_resource_path"
                            phx-value-type={resource_type}
                            class="btn btn-ghost btn-xs"
                          >
                            <.icon name="hero-pencil-square" class="w-4 h-4" />
                          </button>
                          <button
                            type="button"
                            phx-click="remove_resource_path"
                    phx-disable-with={gettext("Removing...")}
                            phx-value-type={resource_type}
                            class="btn btn-ghost btn-xs text-error"
                            data-confirm={
                              gettext("Remove path pattern for \"%{type}\"?", type: resource_type)
                            }
                          >
                            <.icon name="hero-trash" class="w-4 h-4" />
                          </button>
                        </div>
                      </td>
                    </tr>
                  <% end %>
                <% end %>
              </tbody>
            </table>
          </div>

          <%!-- Mobile: card view --%>
          <div class="md:hidden space-y-3 mb-4">
            <%= for {resource_type, config} <- Enum.sort(@resource_paths) do %>
              <% display_path = extract_path(config) %>
              <% display_title = extract_title(config) %>
              <%= if @editing_resource_type == resource_type do %>
                <div
                  id={"edit-path-mobile-#{resource_type}"}
                  phx-hook="PhoenixKitCommentsInsertAtCursor"
                  class="bg-base-200 rounded-lg p-3"
                >
                  <div class="flex items-center justify-between mb-2">
                    <span class="badge badge-ghost badge-sm">{resource_type}</span>
                  </div>
                  <.form
                    for={%{}}
                    phx-submit="save_resource_path"
                    phx-change="live_edit_path"
                    class="space-y-2"
                  >
                    <div>
                      <label class="label py-0">
                        <span class="fieldset-legend text-xs font-semibold">{gettext("Path")}</span>
                      </label>
                      <textarea
                        name="resource_path[path_template]"
                        class="textarea textarea-sm w-full min-h-[4rem]"
                        rows="2"
                        required
                      ><%= @editing_path_value %></textarea>
                    </div>
                    <div>
                      <label class="label py-0">
                        <span class="fieldset-legend text-xs font-semibold">
                          {gettext("Display title")}
                        </span>
                      </label>
                      <input
                        type="text"
                        name="resource_path[title_template]"
                        value={@editing_title_value}
                        placeholder={gettext("e.g. :metadata.slug")}
                        class="input input-sm w-full"
                      />
                    </div>
                    <%= if keys = Map.get(@metadata_keys_by_type, resource_type) do %>
                      <div class="text-xs text-base-content/60 flex flex-wrap items-center gap-1">
                        <span class="font-semibold">{gettext("Available fields:")}</span>
                        <%= for key <- keys do %>
                          <span
                            class="badge badge-ghost badge-xs font-mono gap-0.5"
                            data-field={":metadata.#{key}"}
                          >
                            :metadata.{key}
                          </span>
                        <% end %>
                      </div>
                    <% end %>
                    <div class="flex justify-end gap-2">
                      <button
                        type="button"
                        phx-click="cancel_edit_resource_path"
                        class="btn btn-ghost btn-xs"
                      >
                        {gettext("Cancel")}
                      </button>
                      <button type="submit"
              phx-disable-with={gettext("Saving…")} class="btn btn-primary btn-xs">
                        {gettext("Save")}
                      </button>
                    </div>
                  </.form>
                </div>
              <% else %>
                <div class="bg-base-200 rounded-lg p-3 space-y-1">
                  <div class="flex items-center justify-between">
                    <span class="badge badge-ghost badge-sm">{resource_type}</span>
                    <div class="flex gap-1">
                      <button
                        type="button"
                        phx-click="edit_resource_path"
                        phx-value-type={resource_type}
                        class="btn btn-ghost btn-xs"
                      >
                        <.icon name="hero-pencil-square" class="w-4 h-4" />
                      </button>
                      <button
                        type="button"
                        phx-click="remove_resource_path"
                    phx-disable-with={gettext("Removing...")}
                        phx-value-type={resource_type}
                        class="btn btn-ghost btn-xs text-error"
                        data-confirm={
                          gettext("Remove path pattern for \"%{type}\"?", type: resource_type)
                        }
                      >
                        <.icon name="hero-trash" class="w-4 h-4" />
                      </button>
                    </div>
                  </div>
                  <div>
                    <span class="text-xs text-base-content/50">{gettext("Path:")}</span>
                    <code class="text-xs break-all">{display_path}</code>
                  </div>
                  <%= if display_title != "" do %>
                    <div>
                      <span class="text-xs text-base-content/50">{gettext("Display:")}</span>
                      <code class="text-xs break-all">{display_title}</code>
                    </div>
                  <% end %>
                </div>
              <% end %>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-4 text-base-content/50 text-sm mb-4">
            {gettext("No resource link patterns configured yet.")}
          </div>
        <% end %>

        <%!-- Unconfigured types from DB --%>
        <%= if @unconfigured_types != [] do %>
          <div class="mb-4">
            <h3 class="text-sm font-semibold text-warning mb-2">
              <.icon name="hero-exclamation-triangle" class="w-4 h-4 inline" />
              {gettext("Resource types without link patterns")}
            </h3>
            <div class="space-y-3">
              <%= for resource_type <- @unconfigured_types do %>
                <div
                  id={"draft-path-#{resource_type}"}
                  phx-hook="PhoenixKitCommentsInsertAtCursor"
                  class="bg-base-200 rounded-lg p-3"
                >
                  <.form
                    for={%{}}
                    phx-submit="add_resource_path"
                    phx-change="live_draft_path"
                    class="space-y-2"
                  >
                    <input
                      type="hidden"
                      name="resource_path[resource_type]"
                      value={resource_type}
                    />
                    <div class="flex items-center justify-between">
                      <div class="flex items-center gap-2">
                        <span class="badge badge-warning badge-sm">{resource_type}</span>
                        <span class="text-xs text-base-content/50">
                          {ngettext(
                            "%{count} comment",
                            "%{count} comments",
                            Map.get(@counts_by_type, resource_type, 0)
                          )}
                        </span>
                      </div>
                    </div>
                    <div class="fieldset">
                      <label class="label py-0">
                        <span class="fieldset-legend text-xs font-semibold">{gettext("Path")}</span>
                      </label>
                      <input
                        type="text"
                        name="resource_path[path_template]"
                        value={Map.get(@draft_paths, resource_type, "")}
                        placeholder={"e.g. /#{resource_type}/:uuid"}
                        class="input input-sm w-full"
                        required
                      />
                    </div>
                    <div class="fieldset">
                      <label class="label py-0">
                        <span class="fieldset-legend text-xs font-semibold">
                          {gettext("Display title")}
                        </span>
                      </label>
                      <input
                        type="text"
                        name="resource_path[title_template]"
                        value={Map.get(@draft_titles, resource_type, "")}
                        placeholder={gettext("e.g. :metadata.slug")}
                        class="input input-sm w-full"
                      />
                    </div>
                    <%= if keys = Map.get(@metadata_keys_by_type, resource_type) do %>
                      <div class="text-xs text-base-content/60 flex flex-wrap items-center gap-1">
                        <span class="font-semibold">{gettext("Available fields:")}</span>
                        <%= for key <- keys do %>
                          <span
                            class="badge badge-ghost badge-xs font-mono gap-0.5"
                            data-field={":metadata.#{key}"}
                          >
                            :metadata.{key}
                          </span>
                        <% end %>
                      </div>
                    <% end %>
                    <div class="flex justify-end">
                      <button type="submit"
              phx-disable-with={gettext("Saving…")} class="btn btn-sm btn-outline btn-warning">
                        <.icon name="hero-plus" class="w-4 h-4" /> {gettext("Add")}
                      </button>
                    </div>
                  </.form>
                </div>
              <% end %>
            </div>
          </div>

          <div class="divider text-xs text-base-content/40">{gettext("or add manually")}</div>
        <% end %>

        <%!-- Add new pattern manually --%>
        <div class="bg-base-200 rounded-lg p-3">
          <.form for={%{}} phx-submit="add_resource_path" class="space-y-2">
            <div class="fieldset">
              <label class="label py-0">
                <span class="fieldset-legend text-xs font-semibold">{gettext("Resource Type")}</span>
              </label>
              <input
                type="text"
                name="resource_path[resource_type]"
                placeholder={gettext("e.g. shoes")}
                class="input input-sm w-full"
                required
              />
            </div>
            <div class="fieldset">
              <label class="label py-0">
                <span class="fieldset-legend text-xs font-semibold">{gettext("Path")}</span>
              </label>
              <input
                type="text"
                name="resource_path[path_template]"
                placeholder={gettext("e.g. /order/shoes/:uuid")}
                class="input input-sm w-full"
                required
              />
            </div>
            <div class="fieldset">
              <label class="label py-0">
                <span class="fieldset-legend text-xs font-semibold">{gettext("Display title")}</span>
              </label>
              <input
                type="text"
                name="resource_path[title_template]"
                placeholder={gettext("e.g. :metadata.slug - Ch. :metadata.chapter")}
                class="input input-sm w-full"
              />
            </div>
            <div class="flex justify-end">
              <button type="submit"
              phx-disable-with={gettext("Saving…")} class="btn btn-sm btn-outline btn-primary">
                <.icon name="hero-plus" class="w-4 h-4" /> {gettext("Add")}
              </button>
            </div>
          </.form>
        </div>
      </div>
    </div>
  </div>
</div>
